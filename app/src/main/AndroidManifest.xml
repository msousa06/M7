<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gp.gpproject">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdicionarDocenteActivity" />
        <activity
            android:name=".ListarDocentesActivity"
            android:label="@string/title_activity_listar_docentes"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".AdicionarCategoriaActivity" />
        <activity android:name=".AdicionarDepartamentoActivity" />
        <activity android:name=".EditarDocenteActivity" />
        <activity android:name=".EliminarDocenteActivity" />
        <activity android:name=".ListarVigilanciasActivity" />
        <activity android:name=".AgendarVigilanciaActivity" />
        <activity android:name=".AdicionarDisciplinaActivity" />
        <activity android:name=".EliminarVigilanciaActivity" />
        <activity android:name=".EditarVigilanciaActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RecoverPasswordActivity" />
    </application>


</manifest>
